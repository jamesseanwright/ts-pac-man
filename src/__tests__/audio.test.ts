import { audioPlayerCreator } from '../audio';

describe('createAudioPlayer', () => {
  it('should create an audio player function that fetches the specified tracks and allows them to be played', () => {

  });
});